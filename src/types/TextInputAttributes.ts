export interface TextInputAttributes {
  autocomplete: boolean;
  list: boolean;
  maxlength: number;
  minlength: number;
  pattern: string;
  placeholder: string;
  readonly: boolean;
  required: boolean;
  size: number;
}
